import useUserPayment from "../useUserPayment";
import { render, screen } from "@testing-library/react";
import { describe, it, expect } from "vitest";

describe( "useUserPayment hook", () =>{ 
  it("gets the user payment data", () =>{ 
    
  })
})